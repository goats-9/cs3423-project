#include "parser.tab.hh"
#include <string>

namespace tabulate
{
    std::string translatePos(yy::location l,const std::string &func_call);
    // translation related info
    class translation
    {
    public:
        
    };
}